---
name: sample-skill
description: 请替换成这个 skill 的使用场景，以及它具体帮助完成什么任务。
---

# 概述

用一小段文字说明这个 skill 是做什么的。重点写 agent 应该怎么做，不要写项目背景。

## 适用场景

- 当任务符合这里定义的流程时，使用这个 skill。
- 指令要具体，保证执行时稳定可靠。

## 工作流程

1. 先查看任务上下文和限制条件。
2. 按这个 skill 的核心流程执行。
3. 仅在需要时再读取 `references/` 中的内容。
4. 当 `scripts/` 或 `assets/` 能减少重复劳动或提高稳定性时再使用。

## 附带资源

- `references/`：按需加载的参考资料
- `scripts/`：用于提高稳定性的辅助脚本
- `assets/`：输出时可能会用到的模板或资源文件

## 备注

- 保持这个文件简洁。
- 详细资料尽量放进 `references/`，不要把 `SKILL.md` 写得过大。
